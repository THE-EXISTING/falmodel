name: falmodel
description: A new Flutter package project.
version: 1.2.2

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  faltool:
    git:
      url: https://github.com/THE-EXISTING/faltool.git
      ref: 1.0.8

  #PLATFORM: All
  dio: ^5.5.0+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
